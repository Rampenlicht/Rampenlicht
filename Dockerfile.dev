# Development Dockerfile mit Hot Reload und WebSocket-Support
FROM node:22-alpine

# Arbeitsverzeichnis erstellen
WORKDIR /app

# Package files kopieren
COPY package*.json ./

# Dependencies installieren
RUN npm ci

# Source-Code wird via Volume gemountet (siehe docker-compose.yml)

# Port für Vite Dev Server
EXPOSE 5173

# Health Check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD wget --quiet --tries=1 --spider http://localhost:5173 || exit 1

# Dev Server starten (mit Host 0.0.0.0 für Docker)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]


